

	グラフィックエディター　ＩＳＥ version 1.01 ドキュメント


▼ 内容説明 _______________________________________________________________

	本ソフトウェアは、ＭＳＸｔＲ上で、スクリーン７のインターレース
	画面に作画、編集をするものです。網かけ機能、マスク機能などを備
	え、操作性を向上させています。

▼ 動作環境 _______________________________________________________________

	動作機器	ＭＳＸｔｕｒｂｏＲ以降
	必要機器	未使用セグメント１０枚(160Kbytes)
			ＭＳＸ-ＪＥ(仮想端末機能を有するもの)
			マウス(Ｂトリガのあるもの)
	動作ＯＳ	ＭＳＸ－ＤＯＳ２

	補足	セグメントは、プライマリスロットにある必要はありません。

		RAM非増設の256KbytesRAMのｔｕｒｂｏＲ(A1-ST等)は、R800-
		DRAM-mode ではセグメントが確保できません。あらかじめ、
		システムソフトウェア用のセグメントを開放しておく必要が
		あります。セグメント開放には別途ソフトウェアを使用して
		ください。

		ＭＳＸ-ＪＥは、A1-ST、GTにはメーカーオプションですでに
		搭載されています。

▼ 起動方法 _______________________________________________________________

	ise.com をパスが通っているディレクトリに置き、各専用環境変数を
	設定してください。なお、*.ovlはramdiskに置いた方が快適に使用す
	ることができます。

	コマンドラインから、ise.com を起動させます。

		A>ise

	後は、自動的に他のファイルを呼び出し、作業画面へと移ります。

▼ 専用環境変数 ___________________________________________________________

	ISEOVL	オーバーレイファイルのあるディレクトリを入れる。省略時
		は"A:\"。設定にはドライブ名とディレクトリパスを必ず入れ
		なければならない。またパスはルートディレクトリから指定
		しなければならない。
			 ex. set iseovl=h:\ise\

	ISEENV	マウスの登録をする。始めの１バイトはマウスが接続されて
		いるポート番号、'1'or'2'を、次の１バイトはマウスのトリ
		ガの機能、通常の場合は'N'、トリガのＡＢの機能を取り替え
		る(つまり左利き用)場合は'R'。省略時には、'1N'。

	ISEINT	ise.com 起動時に読み込む.intファイルを登録する。拡張子
		は省略不可。省略時、または登録されたファイルが見つから
		ない場合には無視される。

	ISEUSE	ＭＡＧセーブ時に使用されるユーザー名を入れる。省略時に
		は、環境変数 'USER' が代わりに参照される。'USER' が見つ
		からない場合には、">謎<" とセーブされる。

	ISEMES	ＭＡＧセーブ時に使用されるコメントを入れる。省略時には、
		ise.comのバージョンがセーブされる。

	ISEFEP	ＭＳＸ-ＪＥを選択する。内容は10進数数値。省略時には0。
		仮想端末機能を持たないＪＥは選択してはならない。

	ISEBRK	ise.com が作成するテンポラリ変数。ユーザーが設定したり、
		変更したりすると、システムが破壊される恐れあり。

▼ 使用方法 _______________________________________________________________

	別途配布のマニュアルを参照してください。

▼ 謝辞 ___________________________________________________________________

	本ソフトウェアを開発するにあたって、協力してくださったナツメネッ
	ト会員の方々に、この場を借りてお礼を申し上げます。

▼ 免責 ___________________________________________________________________

	本ソフトウェアの使用による如何なる損害も、作者に責はないものと
	します。

▼ 募金 ___________________________________________________________________

	ISE への寄付を募ります。これはシェアウェアの類いの集金とは異な
	ります。また、サポート等で寄付者、非寄付者と区別をすることはあ
	りません。

	ISE を使用する価値を認め、寄付してもよい(＆寄付する余裕がある)
	という方は、下記の宛先まで寄付を送って頂きたく思います。寄付は、
	金銭、物品どちらでも構いません。

	寄付されたものについては、何等かの形で ISE または作者の製作す
	るソフトウェアを使用するユーザーに還元するつもりです。

	宛先	郵便番号１８３
		　東京都府中市押立町5-14-52内田コーポ201号室
							青木高博

▼ 配布条件 _______________________________________________________________

	本プログラムは、以下を遵守する限り、転載、配布は自由とします。
	ただし作者に連絡がつく場合にはその旨報告して頂ける事を期待します。

		１、アーカイブファイルをそのまま転載、配布する。
		　　アーカイブファイルの変更は許されない。

		２、転載、配布することにより、著作者の権利を侵す場合は、
		　　転載、配布をしない。

		３、商目的の場合には、事前に作者の承認を受ける。

▼ その他 _________________________________________________________________

	本ソフトウェアは、ＦＳＷ(フリーソフトウェア)です。
	使用の対価は一切要求しません。

	本ソフトウェアはＭＳＸ－Ｃ((株)アスキー製)を使用しています。

	著作者は、著作権を保留します。


	使用上での疑問点などは、下記のネットのIDまで連絡してください。

					ナツメネット	NAT26324
					東京ＢＢＳ	GENGOROU

								多摩川 源五郎
