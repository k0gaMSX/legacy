
FFIND                                                    
-----

Format:      FFIND [parameters] <filename>


Purpose:     Finds filåó oî disk.

Use:        Parameters: /A    Scan all drives for file
                        /G    Go to path of first matching file
                        /P    Wait for key if screen is full
                        /T    Search path environment only
                        /E    Search path in FFPATH environment string
                        /D    Dont search drives in FFDRV environment string
                        /Fnnn List only first nnn found files (1-255)