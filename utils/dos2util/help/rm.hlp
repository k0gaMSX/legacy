RM
--

Format:      RM [-adfir] <filename> ...

Purpose:     Removes one or more files, adapted from UNIX.

Use:	     The  filename specifies which files are to be deleted.

               -a
               -d  remove files and show their filename
               -f
               -i  remove files, but first answer question with y/n
               -r  remove (sub)directory including files
